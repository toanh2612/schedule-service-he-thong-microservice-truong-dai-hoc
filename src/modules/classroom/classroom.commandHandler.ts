// import {
//   CommandBus,
//   // EventBus
// } from "@nestjs/cqrs";
// import { RegisterClassroomsCommand } from "./classroom.command";

export default class ClassroomCommandHandler {
  constructor() // private readonly eventBus: EventBus,
  // private readonly commandBus: CommandBus
  {}
  async registerClassroomsCommandHandler() { // registerClassroomsCommand: RegisterClassroomsCommand
    try {
      // const { studentId, classroomIds } = registerClassroomsCommand;
      // await this.commandBus.
    } catch (error) {
      throw error;
    }
  }
}
